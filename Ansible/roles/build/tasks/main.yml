---
# tasks file for build

#buildproject

                - name: build app
                  shell: ng build --prod
                  environment:
                   PATH: /root/.nvm/versions/node/v12.22.7/bin
