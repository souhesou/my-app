---
# tasks file for build

#buildproject

                - name: build app
                  shell: ng build --prod
                  environment:
                   PATH: /home/souha/my-app/node_modules/.bin/
